#!/bin/sh
if [ "$1" = "alac" ]; then
  alacritty --working-directory=$PWD
elif [ "$1" = "naut" ]; then
  nohup nautilus . > /dev/null 2>&1 &
else
  $TERMINAL -d 80 25 --class ranger,Ranger -e $FILE $PWD
fi
